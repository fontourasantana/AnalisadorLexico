while(x>0){
	case A:
	a++;
	x = 0;
	if ( a > b + c ){
		c--;
		for( x = y; x < ++y ; c += 10 );
			a+=b;
			(A -B) > ee;	
			switch(variavel){
				case 2:
					a++;
				case 22:
					b--;
			}
		}
}