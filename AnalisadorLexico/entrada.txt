switch(a){
	case s:
	a++;
}
x = a + b;
b = c;
if ( a > b + c ){
	c--;
	for( x = y; x < ++y ; c += 10 );
		a += b;
		(A -B) > ee;	
		switch(variavel){
			case 2:
				a++;
			case 22:
				b--;
		}
}